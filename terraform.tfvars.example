aws_region = "us-east-1"
project_name = "auto-healing-web-tier"
environment = "dev"
# Two t4g.nano instances and two 8 GiB gp3 volumes are fixed by design.
# variables.tf pins the regional ARM64 AMI for repeatable plans.
